{
  "name": "cltask-eleventy",
  "version": "1.0.0",
  "description": "Card task - create a card component and consume an api that populates the card content",
  "main": "index.js",
  "scripts": {
    "start": "eleventy --watch --serve",
    "build": "eleventy --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dale Hudson",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "dayjs": "^1.10.7"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
